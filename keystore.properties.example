# Copy this file to "keystore.properties" (same folder, project root) and fill in your own
# values. That file is git-ignored — it never gets committed, so your keystore path and
# passwords stay local to your machine.

storeFile=C:\\caminho\\completo\\para\\a\\tua.keystore
storePassword=a-tua-password-da-keystore
keyAlias=o-teu-alias
keyPassword=a-password-desse-alias
